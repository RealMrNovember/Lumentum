name: lumentum_shared
description: Lumentum shared client — modeller, API istemcisi ve yapılandırma. Tüm platformlar bu paketi kullanır.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
